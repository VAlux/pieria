# PDFBox requires Windows-1252 while parsing PDF syntax. Its classes and the logging backend must
# retain their normal runtime initialization; PDFBox 3.0.8 avoids the eager AWT warm-up that
# previously forced PDDocument into build-time initialization (PDFBOX-6214).
Args = -H:+AddAllCharsets
